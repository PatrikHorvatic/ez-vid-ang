import { EvaMediaEventListenersDirective } from './media-event-listeners';

describe('MediaEventListenersDirective', () => {
  it('should create an instance', () => {
    const directive = new EvaMediaEventListenersDirective();
    expect(directive).toBeTruthy();
  });
});
