import { EvaDashDirective } from './dash';

describe('DashDirective', () => {
  it('should create an instance', () => {
    const directive = new EvaDashDirective();
    expect(directive).toBeTruthy();
  });
});
