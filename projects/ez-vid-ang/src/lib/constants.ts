// Time conversion
export const SECONDS_PER_HOUR = 3600;
export const SECONDS_PER_MINUTE = 60;
export const TIME_DISPLAY_PAD_WIDTH = 2;

// Percentage math
export const PERCENTAGE = 100;
export const SCRUB_BAR_MAX_SEEK_PERCENT = 99.9;

// Volume thresholds (0–1 normalized)
export const DEFAULT_LOW_VOLUME_THRESHOLD = 0.25;
export const DEFAULT_MIDDLE_VOLUME_THRESHOLD = 0.75;

// Volume keyboard step sizes (percentage)
export const VOLUME_ARROW_KEY_STEP = 5;
export const VOLUME_PAGE_KEY_STEP = 10;

// Seek duration (seconds)
export const DEFAULT_SEEK_SECONDS = 10;
export const SEEK_ICON_THRESHOLD_30 = 30;

// Number key seek
export const MAX_DIGIT_KEY = 9;
export const DIGIT_DIVISOR = 10;

// Playback speed bounds
export const MIN_PLAYBACK_SPEED = 0.25;
export const MAX_PLAYBACK_SPEED = 4;

// Buffering detection
export const READY_STATE_HAVE_FUTURE_DATA = 3;
export const BUFFERING_DETECTION_DELAY_MS = 500;

// Auto-hide timeout (milliseconds)
export const DEFAULT_AUTOHIDE_TIMEOUT_MS = 3000;

// Screen reader announcement timing (milliseconds)
export const VOLUME_ANNOUNCE_DEBOUNCE_MS = 300;
export const VOLUME_ANNOUNCE_RESET_MS = 100;

// UI interaction timing (milliseconds)
export const POST_DRAG_CLICK_SUPPRESS_MS = 10;
export const SCREEN_READER_ANNOUNCEMENT_DURATION_MS = 1000;
export const BUFFER_UPDATE_THROTTLE_MS = 2000;

// Click-outside debounce (milliseconds)
export const CLICK_OUTSIDE_DEBOUNCE_MS = 50;

// Scrub bar tooltip (pixels)
export const SCRUB_BAR_TOOLTIP_HALF_WIDTH_PX = 35;

// Mouse interaction throttle (milliseconds)
export const MOUSEMOVE_THROTTLE_MS = 100;

// Chapter update debounce (milliseconds)
export const CHAPTER_UPDATE_DEBOUNCE_MS = 300;

// Default seek step for keyboard arrow keys (seconds)
export const DEFAULT_ARROW_SEEK_SECONDS = 5;

// Default unmute volume (0–1 normalized)
export const DEFAULT_UNMUTE_VOLUME = 0.75;
