import { EvaKeyboardShortcuts } from './keyboard-shortcuts';

describe('KeyboardShortcuts', () => {
  it('should create an instance', () => {
    const directive = new EvaKeyboardShortcuts();
    expect(directive).toBeTruthy();
  });
});
