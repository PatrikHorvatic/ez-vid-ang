import { MediaEventListenersDirective } from './media-event-listeners.directive';

describe('MediaEventListenersDirective', () => {
  it('should create an instance', () => {
    const directive = new MediaEventListenersDirective();
    expect(directive).toBeTruthy();
  });
});
